server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

info:
  app:
    name: gateway-service
    description: API Gateway responsible for routing external requests to EventHub microservices
    version: 1.0.0
